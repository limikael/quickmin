import Backroom from "../../src/cli/Backroom.js";
import fs from "fs";

describe("Backroom",()=>{
	it("works",async ()=>{
		/*if (fs.existsSync("spec/tmp/backroom-test.db"))
			fs.unlinkSync("spec/tmp/backroom-test.db")

		let backroom=new Backroom();
		backroom.conf={
			dsn: "sqlite:spec/tmp/backroom-test.db"
		};

		await backroom.sync();

		expect(fs.existsSync("spec/tmp/backroom-test.db")).toBe(true);*/
	})
})